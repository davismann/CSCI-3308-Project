<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipes Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h1 {
            margin-bottom: 20px;
            color: #333;
        }

        .user-info {
            margin-bottom: 20px;
        }

        .user-info p {
            margin: 5px 0;
        }

        .button {
            padding: 10px 20px;
            font-size: 16px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .button:hover {
            background-color: #b3d2f3;
        }
    </style>
</head>

<body>
    {{>message}}

    <button id="myBtn">Open Modal</button>

    <div id="myModal" class="modal">

        <div class="modal-content">
            <span class="close">&times;</span>
            <form id="recipeFiltersForm">
                <h2>Enter Recipe Filters</h2>

                <label for="keyword">Keyword:</label>
                <input type="text" id="keyword" name="keyword">

                <label for="minCal">Minimum Calories:</label>
                <input type="number" id="minCal" name="minCal" min="0" value="100">

                <label for="maxCal">Maximum Calories:</label>
                <input type="number" id="maxCal" name="maxCal" min="0" value="2000">

                <label for="mealType">Meal Type:</label>
                <select id="mealType" name="mealType">
                    <option value="Breakfast">Breakfast</option>
                    <option value="Lunch">Lunch</option>
                    <option value="Dinner" selected>Dinner</option>
                    <option value="Snack">Snack</option>
                    <option value="None">None</option>
                </select>

                <h3>Nutrients</h3>
                <label for="minProtein">Min Protein (g):</label>
                <input type="text" id="minProtein" name="minProtein">

                <label for="maxProtein">Max Protein (g):</label>
                <input type="text" id="maxProtein" name="maxProtein">

                <label for="minCarbs">Min Carbohydrates (g):</label>
                <input type="text" id="minCarbs" name="maxCarbs">

                <label for="maxCarbs">Max Carbohydrates (g):</label>
                <input type="text" id="maxCarbs" name="maxCarbs">

                <label for="minFat">Min Fat (g):</label>
                <input type="text" id="minFat" name="minFat">

                <label for="maxFat">Max Fat (g):</label>
                <input type="text" id="maxFat" name="maxFat">

                <input type="submit" value="Apply Filters">
            </form>
        </div>

    </div>
    <div class="container mx-auto">
        {{#if recipes.length}}
        {{#each recipes}}
        <div class="row justify-content-md-center">
            <div class="col-md-8">
                <h3>{{this.label}}</h3>
                <img src="{{this.image}}" alt="{{this.label}}" style="width: 100%; max-width: 300px;">
                <p>Calories: {{this.calories}} kcal</p>
                <p>Protein: {{this.nutrients.PROCNT.quantity}} g</p>
                <p>Fat: {{this.nutrients.FAT.quantity}} g</p>
                <p>Carbs: {{this.nutrients.CHOCDF.quantity}} g</p>
                <p><a href="{{this.url}}">View Recipe</a></p>
            </div>
        </div>
        {{/each}}
        {{else}}
        <div class="row justify-content-md-center">
            <div class="col">
                No recipes found.
            </div>
        </div>
        {{/if}}
    </div>
    <script type="text/javascript" src="/resources/js/script.js"></script>

</body>

</html>